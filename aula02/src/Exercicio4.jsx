export default function Exercicio4( {tipo, base, altura })
{
    let area = 0;

    if (tipo = triangulo) {
        area = (base * altura) / 2;
    } else if (tipo = retangulo) {
        area = base * altura;
    }
}

